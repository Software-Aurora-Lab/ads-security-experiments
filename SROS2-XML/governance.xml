<?xml version="1.0" encoding="UTF-8"?>
<!--
  governance.xml
  ==============
  Domain-wide DDS security policy.
  - Unauthenticated participants are rejected outright.
  - Access control is enforced on every topic.

  Sign and install with:
    ros2 security generate_artifacts \
      -k ~/autoware_keystore \
      -p autoware_policy.xml
  (generate_artifacts signs this automatically)
-->
<dds xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
     xsi:noNamespaceSchemaLocation="http://www.omg.org/spec/DDS-SECURITY/20170901/omg_shared_ca_governance.xsd">
  <domain_access_rules>
    <domain_rule>
      <domains>
        <id>0</id>
      </domains>

      <!-- Enforce per-enclave permissions from permissions.xml -->
      <enable_join_access_control>true</enable_join_access_control>

      <!-- Encrypt discovery so unknown nodes can't enumerate the graph -->
      <discovery_protection_kind>ENCRYPT</discovery_protection_kind>
      <liveliness_protection_kind>ENCRYPT</liveliness_protection_kind>
      <rtps_protection_kind>ENCRYPT</rtps_protection_kind>

      <topic_access_rules>

        <!-- All topics: access-controlled and encrypted -->
        <topic_rule>
          <topic_expression>*</topic_expression>
          <enable_discovery_protection>true</enable_discovery_protection>
          <enable_liveliness_protection>true</enable_liveliness_protection>
          <enable_read_access_control>true</enable_read_access_control>
          <enable_write_access_control>true</enable_write_access_control>
        </topic_rule>

      </topic_access_rules>
    </domain_rule>
  </domain_access_rules>
</dds>
